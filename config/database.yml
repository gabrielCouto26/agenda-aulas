default: &default
  adapter: mysql2
  encoding: utf8
  username: root
  password: root
  host: localhost
  port: 3306

development:
  <<: *default
  database: db_agenda_aulas

test:
  <<: *default
  database: db_test